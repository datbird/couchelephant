<?xml version="1.0"?>
<Container version="2">
  <Name>CouchElephant</Name>
  <Repository>ghcr.io/datbird/couchelephant:latest</Repository>
  <Registry>https://github.com/datbird/couchelephant/pkgs/container/couchelephant</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/datbird/couchelephant/issues</Support>
  <Project>https://github.com/datbird/couchelephant</Project>
  <Overview>
    Plex's DVR records the wrong broadcast. Ask it to record your team and it
    will happily pick the repeat that airs two days later, on a channel you do
    not watch, and tell you nothing about it.&#13;&#13;
    CouchElephant reads the same guide, picks the live broadcast itself, and
    pins the recording to that exact channel and start time so Plex has nothing
    left to choose. Recordings land in your Plex library exactly as they always
    did.&#13;&#13;
    It also gives you passes Plex has no equivalent for: follow any of 1,300+
    professional and college teams, or build a smart filter out of nested AND,
    OR and NOT conditions over genre, rating, channel, time and more. Every
    standard Plex recording option, padding included, is on every pass.&#13;&#13;
    Point it at your Plex server in Settings and nothing else is required.
    A new install starts in preview mode: it shows you which airing each rule
    would take without writing anything to your DVR.
  </Overview>
  <Category>MediaApp:Video Tools:Utilities</Category>
  <WebUI>http://[IP]:[PORT:8710]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/datbird/couchelephant/main/unraid/couchelephant.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/datbird/couchelephant/main/unraid/couchelephant.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>A Plex Media Server with a DVR and a tuner already set up.</Requires>

  <Config Name="WebUI Port" Target="8710" Default="8710" Mode="tcp"
          Description="The port the web interface listens on."
          Type="Port" Display="always" Required="true" Mask="false">8710</Config>

  <Config Name="Appdata" Target="/data" Default="/mnt/user/appdata/couchelephant" Mode="rw"
          Description="The database, your channel artwork, and any snapshots you schedule. The guide is a cache and rebuilds itself, so this stays small."
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/couchelephant</Config>

  <Config Name="TZ" Target="TZ" Default="UTC" Mode=""
          Description="The container's clock. Guide times are stored as UTC and shown in the timezone you pick in Settings, so this only affects the log."
          Type="Variable" Display="always" Required="false" Mask="false">UTC</Config>
</Container>
